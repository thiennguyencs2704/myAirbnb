// For cards on the Homepage

export type NearbyLocation = {
  img: string;
  location: string;
  distance: string;
};

export type AnywhereCategory = {
  category: string;
  img: string;
};

export type DiscoverThing = {
  title: string;
  discription: string;
  img: string;
};

// For Footer

export type FooterType = {
  category: string;
  items: string[];
};
